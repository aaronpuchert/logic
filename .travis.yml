language: cpp
compiler:
  - gcc
  - clang
sudo: false
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-4.9
      - clang-3.6
      - libstdc++-4.8-dev
      - libboost-test-dev
script:
  - make -k all
  - make -k test
